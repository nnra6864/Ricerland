// white: fallback for background
windowBg: #ricer.col.background;

// black: fallback for text
windowFg: #ricer.col.foreground;

// light gray: fallback for background with mouse over
windowBgOver: #ricer.col.background_highlight;

// darker gray: fallback for ripple effect
windowBgRipple: #ricer.col.background_alt;

// black: fallback for text with mouse over
windowFgOver: #ricer.col.foreground_highlight;

// gray: fallback for additional text
windowSubTextFg: #ricer.col.foreground_alt;

// darker gray: fallback for additional text with mouse over
windowSubTextFgOver: #ricer.col.foreground_highlight;

// dark gray: fallback for bold text
windowBoldFg: #ricer.col.foreground;

// dark gray: fallback for bold text with mouse over
windowBoldFgOver: #ricer.col.foreground_highlight;

// bright blue: fallback for blue filled active areas
windowBgActive: #ricer.col.background;

// white: fallback for text on active areas
windowFgActive: #ricer.col.foreground;

// online blue: fallback for active text like online status
windowActiveTextFg: #ricer.col.foreground_alt;

// black: fallback for shadow
windowShadowFg: #ricer.col.shadow;

// gray: fallback for shadow without opacity
windowShadowFgFallback: #ricer.col.shadow;

// most shadows (including opacity)
shadowFg: #ricer.col.shadow\56;

// slide animation (chat to profile) fade out filling
slideFadeOutBg: #0000003c;

// slide animation (chat to profile) fade out right section shadow
slideFadeOutShadowFg: windowShadowFg;

// image background fallback (when photo size is less than minimum allowed)
imageBg: #ricer.col.background;

// image background when displaying an image with opacity where no opacity is needed
imageBgTransparent: #ffffff;

// default active button background
activeButtonBg: #ricer.col.button;

// default active button background with mouse over
activeButtonBgOver: #ricer.col.button_highlight;

// default active button ripple effect
activeButtonBgRipple: #ricer.col.button_highlight;

// default active button text
activeButtonFg: #ricer.col.button_foreground;

// default active button text with mouse over
activeButtonFgOver: #ricer.col.button_foreground_highlight;

// default active button additional text (selected messages counter in forward / delete buttons)
activeButtonSecondaryFg: #ricer.col.button_foreground;

// default active button additional text with mouse over
activeButtonSecondaryFgOver: activeButtonSecondaryFg;

// default active line (like code input field bottom border when you log in and field is focused)
activeLineFg: #ricer.col.foreground;

// default active line for error state (like code input field bottom border when you log in and you've entered incorrect code)
activeLineFgError: #ricer.col.red;

// default light button background (like buttons in boxes)
lightButtonBg: #ricer.col.button;

// default light button background with mouse over
lightButtonBgOver: #ricer.col.button_highlight;

// default light button ripple effect
lightButtonBgRipple: #ricer.col.button_highlight;

// default light button text
lightButtonFg: #ricer.col.button_foreground;

// default light button text with mouse over
lightButtonFgOver: lightButtonFg;

// default attention button text (like confirm button on log out)
attentionButtonFg: #ricer.col.button_highlight;

// default attention button text with mouse over
attentionButtonFgOver: #ricer.col.button_foreground;

// default attention button background with mouse over
attentionButtonBgOver: #ricer.col.red\64;

// default attention button ripple effect
attentionButtonBgRipple: #ricecr.col.br_red\64;

// default left outlined button background (like shared media links in profiles)
outlineButtonBg: windowBg;

// default left outlined button background with mouse over
outlineButtonBgOver: #ricer.col.button;

// default left outlined button left outline border
outlineButtonOutlineFg: #ricer.col.button_foreground;

// default left outlined button ripple effect
outlineButtonBgRipple: #ricer.col.button_highlight;

// default popup menu background
menuBg: #ricer.col.background;

// default popup menu item background with mouse over
menuBgOver: #ricer.col.background_highlight;

// default popup menu item ripple effect
menuBgRipple: #ricer.col.background_alt;

// default popup menu item icon (like main menu)
menuIconFg: #ricer.col.foreground;

// default popup menu item icon with mouse over
menuIconFgOver: #ricer.col.foreground;

// default popup menu submenu arrow icon (like in message field context menu in case of RTL system language)
menuSubmenuArrowFg: #ricer.col.foreground;

// default popup menu item disabled text (like unavailable items in message field context menu)
menuFgDisabled: #ricer.col.foreground_disabled;

// default popup menu separator (like in message field context menu)
menuSeparatorFg: #ricer.col.outline;

// default scroll bar current rectangle, the bar itself (like in chats list)
scrollBarBg: #ricer.col.foreground\53;

// default scroll bar current rectangle with mouse over it
scrollBarBgOver: #ricer.col.foreground_highlight\7a;

// default scroll bar background
scrollBg: #ricer.col.background\1a;

// default scroll bar background with mouse over the scroll bar
scrollBgOver: #ricer.col.background_highlight\2c;

// small X icon (like in Show all sessions box to the right for sessions termination)
smallCloseIconFg: #ricer.col.foreground_alt;

// small X icon with mouse over
smallCloseIconFgOver: #ricer.col.foreground_highlight_alt;

// default radial loader line (like in Media Viewer when loading a photo)
radialFg: windowFgActive;

// default radial loader background (like in Media Viewer when loading a photo)
radialBg: #00000056;

// default input field placeholder when field is not focused (like in phone input field when you log in)
placeholderFg: #ricer.col.foreground_placeholder;

// default input field placeholder when field is focused
placeholderFgActive: #ricer.col.foreground_placeholder;

// default input field bottom border (like in code input field when you log in and field is not focused)
inputBorderFg: #ricer.col.outline;

// default rounded input field border (like in chats list search field when field is focused)
filterInputBorderFg: #ricer.col.outline;

// default rounded input field background (like in chats list search field when field is inactive)
filterInputInactiveBg: #ricer.col.button_inactive;

// default unchecked checkbox rounded rectangle (and also emoji category icons)
checkboxFg: #ricer.col.foreground_alt;

// default slider not active bar (like in Settings when you choose interface scale or custom notifications count)
sliderBgInactive: #ricer.col.background;

// default slider active bar (like in Settings when you choose interface scale or custom notifications count)
sliderBgActive: #ricer.col.foreground;

// tooltip background (like when you put mouse over the message timestamp and wait)
tooltipBg: #ricer.col.background;

// tooltip text
tooltipFg: #ricer.col.foreground;

// tooltip border
tooltipBorderFg: #ricer.col.outline;

// one pixel line shadow at the bottom of custom window title
titleShadow: #00000000;

// custom window title background when window is inactive
titleBg: #ricer.col.background;

// custom window title background when window is active
titleBgActive: #ricer.col.background;

// custom window title minimize/maximize/restore button background when window is inactive (Windows only)
titleButtonBg: titleBg;

// custom window title minimize/maximize/restore button icon when window is inactive (Windows only)
titleButtonFg: #ricer.col.button_foreground;

// custom window title minimize/maximize/restore button background with mouse over when window is inactive (Windows only)
titleButtonBgOver: #ricer.col.button_highlight;

// custom window title minimize/maximize/restore button icon with mouse over when window is inactive (Windows only)
titleButtonFgOver: #ricer.col.button_foreground_highlight;

// custom window title minimize/maximize/restore button background when window is active (Windows only)
titleButtonBgActive: #ricer.col.button;

// custom window title minimize/maximize/restore button icon when window is active (Windows only)
titleButtonFgActive: titleButtonFg;

// custom window title minimize/maximize/restore button background with mouse over when window is active (Windows only)
titleButtonBgActiveOver: #ricer.col.button_highlight;

// custom window title minimize/maximize/restore button icon with mouse over when window is active (Windows only)
titleButtonFgActiveOver: titleButtonFgOver;

// custom window title close button background when window is inactive (Windows only)
titleButtonCloseBg: titleButtonBg;

// custom window title close button icon when window is inactive (Windows only)
titleButtonCloseFg: titleButtonFg;

// custom window title close button background with mouse over when window is inactive (Windows only)
titleButtonCloseBgOver: #ricer.col.button_highlight;

// custom window title close button icon with mouse over when window is inactive (Windows only)
titleButtonCloseFgOver: windowFgActive;

// custom window title close button background when window is active (Windows only)
titleButtonCloseBgActive: #ricer.col.button;

// custom window title close button icon when window is active (Windows only)
titleButtonCloseFgActive: titleButtonCloseFg;

// custom window title close button background with mouse over when window is active (Windows only)
titleButtonCloseBgActiveOver: #ricer.col.background_highlight;

// custom window title close button icon with mouse over when window is active (Windows only)
titleButtonCloseFgActiveOver: titleButtonCloseFgOver;

// custom window title text when window is inactive (macOS only)
titleFg: #ricer.col.foreground;

// custom window title text when window is active (macOS only)
titleFgActive: #ricer.col.foreground;

// tray icon counter background
trayCounterBg: #ricer.col.foreground;

// tray icon counter background if all unread messages are muted
trayCounterBgMute: #ricer.col.foreground_disabled;

// tray icon counter text
trayCounterFg: #ricer.col.background;

// tray icon counter background when tray icon is pressed or when dark theme of macOS is used (macOS only)
trayCounterBgMacInvert: #ricer.col.foreground;

// tray icon counter text when tray icon is pressed or when dark theme of macOS is used (macOS only)
trayCounterFgMacInvert: #ricer.col.background\01;

// box and main menu background layer fade
layerBg: #0000007f;

// default for settings close icon and box search cancel icon
cancelIconFg: #ricer.col.red;

// default for settings close icon and box search cancel icon with mouse over
cancelIconFgOver: #ricer.col.br_red;

// box background
boxBg: #ricer.col.background;

// box text
boxTextFg: windowFg;

// accepted box text (like when choosing username that is not occupied)
boxTextFgGood: #ricer.col.foreground;

// rejecting box text (like when choosing username that is occupied)
boxTextFgError: #ricer.col.br_red;

// box title text
boxTitleFg: #ricer.col.foreground;

// box search field background (like in contacts box)
boxSearchBg: #ricer.col.button;

// box title additional text (like in create group box when you see chosen members count)
boxTitleAdditionalFg: #ricer.col.foreground_alt;

// settings close icon and box search cancel icon (like in contacts box)
boxTitleCloseFg: cancelIconFg;

// settings close icon and box search cancel icon (like in contacts box) with mouse over
boxTitleCloseFgOver: cancelIconFgOver;

// text in channel members box about the limit (max 200 last members are shown)
membersAboutLimitFg: #ricer.col.foreground_alt;

// contacts (and some other) box row background
contactsBg: #ricer.col.background;

// contacts (and some other) box row background with mouse over
contactsBgOver: #ricer.col.background_highlight;

// contacts (and some other) box row name text
contactsNameFg: boxTextFg;

// contacts (and some other) box row additional text (like last seen stamp)
contactsStatusFg: #ricer.col.foreground_alt;

// contacts (and some other) box row additional text (like last seen stamp) with mouse over
contactsStatusFgOver: #ricer.col.foreground_highlight_alt;

// contacts (and some other) box row active additional text (like online status)
contactsStatusFgOnline: #ricer.col.foreground_alt;

// avatar crop box fade background (when choosing a new photo in Settings or for a group)
photoCropFadeBg: layerBg;

// avatar crop box corner rectangles (when choosing a new photo in Settings or for a group)
photoCropPointFg: #ricer.col.background\7f;

// received phone call arrow (in calls list box)
callArrowFg: #ricer.col.foreground;

// missed phone call arrow (in calls list box)
callArrowMissedFg: #ricer.col.red;

// login background
introBg: windowBg;

// login title text
introTitleFg: #ricer.col.foreground;

// login description text
introDescriptionFg: #ricer.col.foreground;

// login error text (like when providing a wrong log in code)
introErrorFg: #ricer.col.red;

// intro gradient top (from)
introCoverTopBg: #ricer.col.background;

// intro gradient bottom (to)
introCoverBottomBg: #ricer.col.background_alt;

// intro cloud graphics
introCoverIconsFg: #ricer.col.foreground;

// intro plane traces
introCoverPlaneTrace: #ricer.col.foreground;

// intro plane part
introCoverPlaneInner: #ricer.col.foreground;

// intro plane part
introCoverPlaneOuter: #ricer.col.foreground_highlight;

// intro plane part
introCoverPlaneTop: #ricer.col.foreground_highlight;

// main menu and lock telegram icon
dialogsMenuIconFg: menuIconFg;

// main menu and lock telegram icon with mouse over
dialogsMenuIconFgOver: menuIconFgOver;

// chat list background
dialogsBg: windowBg;

// chat list name text
dialogsNameFg: #ricer.col.foreground;

// chat list group or channel icon
dialogsChatIconFg: dialogsNameFg;

// chat list date text
dialogsDateFg: #ricer.col.foreground_alt;

// chat list message text
dialogsTextFg: #ricer.col.foreground;

// chat list group sender name text (or media message type text)
dialogsTextFgService: #ricer.col.foreground;

// chat list draft label
dialogsDraftFg: #ricer.col.foreground_placeholder;

// chat list verified icon background
dialogsVerifiedIconBg: #ricer.col.background;

// chat list verified icon check
dialogsVerifiedIconFg: #ricer.col.foreground_alt;

// chat list sending message icon (clock)
dialogsSendingIconFg: #ricer.col.foreground;

// GOT HERE
// chat list sent message tick / double tick icon
dialogsSentIconFg: #0e93ff;

// chat list unread badge background for not muted chat
dialogsUnreadBg: #0189f8;

// chat list unread badge background for muted chat
dialogsUnreadBgMuted: #285b8c;

// chat list unread badge text
dialogsUnreadFg: #ffffff;

// chat list background with mouse over
dialogsBgOver: #152f47;

// chat list online status
dialogsOnlineBadgeFg: #018dff;

// chat list name text with mouse over
dialogsNameFgOver: windowBoldFgOver;

// chat list group or channel icon with mouse over
dialogsChatIconFgOver: dialogsNameFgOver;

// chat list date text with mouse over
dialogsDateFgOver: #8bb4df;

// chat list message text with mouse over
dialogsTextFgOver: #9dc7ef;

// chat list group sender name text with mouse over
dialogsTextFgServiceOver: #169aff;

// chat list draft label with mouse over
dialogsDraftFgOver: dialogsDraftFg;

// chat list verified icon background with mouse over
dialogsVerifiedIconBgOver: #078fff;

// chat list verified icon check with mouse over
dialogsVerifiedIconFgOver: dialogsVerifiedIconFg;

// chat list sending message icon (clock) with mouse over
dialogsSendingIconFgOver: dialogsSendingIconFg;

// chat list sent message tick / double tick icon with mouse over
dialogsSentIconFgOver: #0e93ff;

// chat list unread badge background for not muted chat with mouse over
dialogsUnreadBgOver: #0189f8;

// chat list unread badge background for muted chat with mouse over
dialogsUnreadBgMutedOver: #516981;

// chat list unread badge text with mouse over
dialogsUnreadFgOver: dialogsUnreadFg;

// chat list background for current (active) chat
dialogsBgActive: #01559e;

// chat list name text for current (active) chat
dialogsNameFgActive: windowFgActive;

// chat list group or channel icon for current (active) chat
dialogsChatIconFgActive: dialogsNameFgActive;

// chat list date text for current (active) chat
dialogsDateFgActive: windowFgActive;

// chat list message text for current (active) chat
dialogsTextFgActive: windowFgActive;

// chat list group sender name text for current (active) chat
dialogsTextFgServiceActive: dialogsTextFgActive;

// chat list draft label for current (active) chat
dialogsDraftFgActive: #7ebcff;

// chat list verified icon background for current (active) chat
dialogsVerifiedIconBgActive: dialogsTextFgActive;

// chat list verified icon check for current (active) chat
dialogsVerifiedIconFgActive: dialogsBgActive;

// chat list sending message icon (clock) for current (active) chat
dialogsSendingIconFgActive: #ffffff99;

// chat list sent message tick / double tick icon for current (active) chat
dialogsSentIconFgActive: dialogsTextFgActive;

// chat list unread badge background for not muted chat for current (active) chat
dialogsUnreadBgActive: dialogsTextFgActive;

// chat list unread badge background for muted chat for current (active) chat
dialogsUnreadBgMutedActive: #51aeff;

// chat list unread badge text for current (active) chat
dialogsUnreadFgActive: #01579e;

// chat list online status for current (active) chat
dialogsOnlineBadgeFgActive: #ffffff;

// 
dialogsRippleBg: #1a3650;

// 
dialogsRippleBgActive: #015da9;

// forwarding panel background (when forwarding messages in the smallest window size)
dialogsForwardBg: dialogsBgActive;

// forwarding panel text (when forwarding messages in the smallest window size)
dialogsForwardFg: dialogsNameFgActive;

// search results bar background (in chats list, contacts box..)
searchedBarBg: #132d49;

// search results bar text (in chats list, contacts box..)
searchedBarFg: #a3bed8;

// top bar background (in chat view, media overview..)
topBarBg: #0b2238;

// emoji panel background
emojiPanBg: windowBg;

// emoji panel categories background
emojiPanCategories: #0b2238;

// emoji panel section header text
emojiPanHeaderFg: #83abd0;

// emoji panel section header background
emojiPanHeaderBg: #fffffff2;

// delete X button background for custom sent stickers in stickers panel (legacy)
stickerPanDeleteBg: #000000cc;

// delete X button icon for custom sent stickers in stickers panel (legacy)
stickerPanDeleteFg: windowFgActive;

// sticker and GIF preview background (when you press and hold on a sticker)
stickerPreviewBg: #000000b0;

// inbox message text
historyTextInFg: windowFg;

// inbox message selected text or text in a selected message
historyTextInFgSelected: #ffffff;

// outbox message text
historyTextOutFg: #e6f5ff;

// outbox message selected text or text in a selected message
historyTextOutFgSelected: #ffffff;

// inbox message link
historyLinkInFg: #119cff;

// inbox message link in a selected text or message
historyLinkInFgSelected: #6dc7ff;

// outbox message link
historyLinkOutFg: #29a9ff;

// outbox message link in a selected text or message
historyLinkOutFgSelected: #6dc7ff;

// inbox media filename text
historyFileNameInFg: historyTextInFg;

// inbox media filename text in a selected message
historyFileNameInFgSelected: #ffffff;

// outbox media filename text
historyFileNameOutFg: historyTextOutFg;

// outbox media filename text in a selected message
historyFileNameOutFgSelected: #ffffff;

// outbox message tick / double tick icon
historyOutIconFg: #0199ff;

// outbox message tick / double tick icon in a selected message
historyOutIconFgSelected: #ffffff;

// media message tick / double tick icon (like in sent photo)
historyIconFgInverted: #ffffffe5;

// outbox sending message icon (clock)
historySendingOutIconFg: #32a3ff;

// inbox sending message icon (clock) (like in sent messages to yourself or in sent messages to a channel)
historySendingInIconFg: #87a6b7;

// media sending message icon (clock) (like in sent photo)
historySendingInvertedIconFg: #ffffffc8;

// received phone call arrow
historyCallArrowInFg: #0184ff;

// received phone call arrow in a selected message
historyCallArrowInFgSelected: #ffffff;

// missed phone call arrow
historyCallArrowMissedInFg: callArrowMissedFg;

// missed phone call arrow in a selected message
historyCallArrowMissedInFgSelected: #ffffff;

// outgoing phone call arrow
historyCallArrowOutFg: #ffffff;

// outgoing phone call arrow
historyCallArrowOutFgSelected: #ffffff;

// new unread messages bar background
historyUnreadBarBg: #062243;

// new unread messages bar shadow
historyUnreadBarBorder: #00091100;

// new unread messages bar text
historyUnreadBarFg: #ffffff;

// forwarding messages in a large window size "choose recipient" background
historyForwardChooseBg: #0000004c;

// forwarding messages in a large window size "choose recipient" text
historyForwardChooseFg: windowFgActive;

// red group member name
historyPeer1NameFg: #fb6169;

// red group member name in a selected message
historyPeer1NameFgSelected: #ffffff;

// red userpic background
historyPeer1UserpicBg: #ff845e;

// green group member name
historyPeer2NameFg: #85de85;

// green group member name in a selected message
historyPeer2NameFgSelected: #ffffff;

// green userpic background
historyPeer2UserpicBg: #9ad164;

// yellow group member name
historyPeer3NameFg: #f3bc5c;

// yellow group member name in a selected message
historyPeer3NameFgSelected: #ffffff;

// yellow userpic background (actually unused)
historyPeer3UserpicBg: #e5ca77;

// blue group member name
historyPeer4NameFg: #01a6ff;

// blue group member name in a selected message
historyPeer4NameFgSelected: #ffffff;

// blue userpic background
historyPeer4UserpicBg: #018dff;

// purple group member name
historyPeer5NameFg: #b48bf2;

// purple group member name in a selected message
historyPeer5NameFgSelected: #ffffff;

// purple userpic background
historyPeer5UserpicBg: #b694f9;

// pink group member name
historyPeer6NameFg: #ff5694;

// pink group member name in a selected message
historyPeer6NameFgSelected: #ffffff;

// pink userpic background
historyPeer6UserpicBg: #ff8aac;

// sea group member name
historyPeer7NameFg: #62d4e3;

// sea group member name in a selected message
historyPeer7NameFgSelected: #ffffff;

// sea userpic background
historyPeer7UserpicBg: #5bcbe3;

// orange group member name
historyPeer8NameFg: #faa357;

// orange group member name in a selected message
historyPeer8NameFgSelected: #ffffff;

// orange userpic background
historyPeer8UserpicBg: #febb5b;

// default userpic initials
historyPeerUserpicFg: windowFgActive;

// the second red userpic background
historyPeer1UserpicBg2: #d45246;

// the second green userpic background
historyPeer2UserpicBg2: #46ba43;

// the second yellow userpic background (actually unused)
historyPeer3UserpicBg2: #e5ca77;

// the second blue userpic background
historyPeer4UserpicBg2: #018dff;

// the second purple userpic background
historyPeer5UserpicBg2: #6c61df;

// the second pink userpic background
historyPeer6UserpicBg2: #d95574;

// the second sea userpic background
historyPeer7UserpicBg2: #01aaff;

// the second orange userpic background
historyPeer8UserpicBg2: #f68136;

// the second saved messages userpic background
historyPeerSavedMessagesBg2: historyPeer4UserpicBg2;

// scroll bar current rectangle, the bar itself in the chat view (adjusted)
historyScrollBarBg: #9eaab57a;

// scroll bar current rectangle with mouse over it in the chat view (adjusted)
historyScrollBarBgOver: #7c858ebc;

// scroll bar background (adjusted)
historyScrollBg: #6a737c4c;

// scroll bar background with mouse over the scroll bar (adjusted)
historyScrollBgOver: #7077806b;

// inbox message background
msgInBg: #062443;

// inbox selected message background (and background of selected text in those messages)
msgInBgSelected: #0180da;

// outbox message background
msgOutBg: #01559e;

// outbox selected message background (and background of selected text in those messages)
msgOutBgSelected: #0180da;

// overlay which is filling the media parts of selected messages (like in selected photo message)
msgSelectOverlay: #0188ff4c;

// overlay which is filling the selected sticker message
msgStickerOverlay: #0188ff7f;

// inbox message information text (like information about a forwarded message original sender)
msgInServiceFg: #0e93ff;

// inbox selected message information text (like information about a forwarded message original sender)
msgInServiceFgSelected: #ffffff;

// outbox message information text (like information about a forwarded message original sender)
msgOutServiceFg: #40a9ff;

// outbox message information text (like information about a forwarded message original sender)
msgOutServiceFgSelected: #ffffff;

// inbox message shadow (below the bubble)
msgInShadow: #6eabd600;

// inbox selected message shadow (below the bubble)
msgInShadowSelected: #0a98f700;

// outbox message shadow (below the bubble)
msgOutShadow: #00000000;

// outbox selected message shadow (below the bubble)
msgOutShadowSelected: #0172db00;

// inbox message time text
msgInDateFg: #6f99bc;

// inbox selected message time text
msgInDateFgSelected: #ffffff;

// outbox message time text
msgOutDateFg: #4ca8ff;

// outbox selected message time text
msgOutDateFgSelected: #ffffff;

// service message text (like date dividers or service message about the group title being changed)
msgServiceFg: windowFgActive;

// service message background (like in a service message about group title being changed) (adjusted)
msgServiceBg: #0e3254d5;

// service message selected text background (like in a service message about group title being changed) (adjusted)
msgServiceBgSelected: #018fed;

// inbox message reply outline
msgInReplyBarColor: #0199ff;

// inbox selected message reply outline
msgInReplyBarSelColor: #ffffff;

// outbox message reply outline
msgOutReplyBarColor: #019eff;

// outbox selected message reply outline
msgOutReplyBarSelColor: #ffffff;

// sticker message reply outline
msgImgReplyBarColor: msgServiceFg;

// inbox message monospace text (like a message sent with `test` text)
msgInMonoFg: #1d95f1;

// outbox message monospace text
msgOutMonoFg: #83c5ff;

// inbox message monospace text in a selected text or message
msgInMonoFgSelected: #69b7ff;

// outbox message monospace text in a selected text or message
msgOutMonoFgSelected: #69b7ff;

// media message time text (like time text in a sent photo)
msgDateImgFg: #fffffff2;

// media message time bubble background (like time bubble in a sent photo) or file with thumbnail download icon circle background
msgDateImgBg: #00000054;

// media message download icon circle background with mouse over (like file with thumbnail download icon)
msgDateImgBgOver: #00000074;

// selected media message time bubble background
msgDateImgBgSelected: #015a9e87;

// inbox media file message with thumbnail download / open with button text
msgFileThumbLinkInFg: lightButtonFg;

// inbox selected media file message with thumbnail download / open with button text
msgFileThumbLinkInFgSelected: #ffffff;

// outbox media file message with thumbnail download / open with button text
msgFileThumbLinkOutFg: #2d9dff;

// outbox selected media file message with thumbnail download / open with button text
msgFileThumbLinkOutFgSelected: #ffffff;

// inbox audio file download circle background
msgFileInBg: #019eff;

// inbox audio file download circle background with mouse over
msgFileInBgOver: #01a2ff;

// inbox selected audio file download circle background
msgFileInBgSelected: #0794ff;

// outbox audio file download circle background
msgFileOutBg: #018dff;

// outbox audio file download circle background with mouse over
msgFileOutBgOver: #0191ff;

// outbox selected audio file download circle background
msgFileOutBgSelected: #0191ff;

// blue shared links / files without image square thumbnail
msgFile1Bg: #0183f5;

// blue shared files without image download circle background
msgFile1BgDark: #0176d0;

// blue shared files without image download circle background with mouse over
msgFile1BgOver: #0171c2;

// blue shared files without image download circle background if file is selected
msgFile1BgSelected: #ffffff;

// green shared links / shared files without image square thumbnail
msgFile2Bg: #3ea34a;

// green shared files without image download circle background
msgFile2BgDark: #298835;

// green shared files without image download circle background with mouse over
msgFile2BgOver: #1b7725;

// green shared files without image download circle background if file is selected
msgFile2BgSelected: #1b7725;

// red shared links / shared files without image square thumbnail
msgFile3Bg: #d6454c;

// red shared files without image download circle background
msgFile3BgDark: #bf333a;

// red shared files without image download circle background with mouse over
msgFile3BgOver: #b2282f;

// red shared files without image download circle background if file is selected
msgFile3BgSelected: #b2282f;

// yellow shared links / shared files without image square thumbnail
msgFile4Bg: #d99546;

// yellow shared files without image download circle background
msgFile4BgDark: #c17d39;

// yellow shared files without image download circle background with mouse over
msgFile4BgOver: #ac6b29;

// yellow shared files without image download circle background if file is selected
msgFile4BgSelected: #ac6b29;

// inbox file without thumbnail (like audio file) download arrow icon
historyFileInIconFg: #ffffff;

// inbox selected file without thumbnail (like audio file) download arrow icon
historyFileInIconFgSelected: #ffffff;

// inbox file without thumbnail (like audio file) radial download animation line
historyFileInRadialFg: #ffffff;

// inbox selected file without thumbnail (like audio file) radial download animation line
historyFileInRadialFgSelected: historyFileInIconFgSelected;

// outbox file without thumbnail (like audio file) download arrow icon
historyFileOutIconFg: #ffffff;

// outbox selected file without thumbnail (like audio file) download arrow icon
historyFileOutIconFgSelected: #ffffff;

// outbox file without thumbnail (like audio file) radial download animation line
historyFileOutRadialFg: #ffffff;

// outbox selected file without thumbnail (like audio file) radial download animation line
historyFileOutRadialFgSelected: #ffffff;

// file with thumbnail (or photo / video) download arrow icon
historyFileThumbIconFg: #efefef;

// selected file with thumbnail (or photo / video) download arrow icon
historyFileThumbIconFgSelected: #ffffff;

// file with thumbnail (or photo / video) radial download animation line
historyFileThumbRadialFg: historyFileThumbIconFg;

// selected file with thumbnail (or photo / video) radial download animation line
historyFileThumbRadialFgSelected: #ffffff;

// radial playback progress in round video messages
historyVideoMessageProgressFg: historyFileThumbIconFg;

// inbox voice message active waveform lines (like played part of currently playing voice message)
msgWaveformInActive: #0191ff;

// inbox selected voice message active waveform lines (like played part of currently playing voice message)
msgWaveformInActiveSelected: #ffffff;

// inbox voice message inactive waveform lines (like upcoming part of currently playing voice message)
msgWaveformInInactive: #275580;

// inbox selected voice message inactive waveform lines (like upcoming part of currently playing voice message)
msgWaveformInInactiveSelected: #2ea4ff;

// outbox voice message active waveform lines (like played part of currently playing voice message)
msgWaveformOutActive: #0188ff;

// outbox selected voice message active waveform lines (like played part of currently playing voice message)
msgWaveformOutActiveSelected: #ffffff;

// outbox voice message inactive waveform lines (like upcoming part of currently playing voice message)
msgWaveformOutInactive: #017aec;

// outbox selected voice message inactive waveform lines (like upcoming part of currently playing voice message)
msgWaveformOutInactiveSelected: #2ea4ff;

// this is painted over a bot inline keyboard button (which has msgServiceBg background) when mouse is over that button
msgBotKbOverBgAdd: #48b0ff0f;

// bot inline keyboard button icon in the top-right corner (like in @vote bot when a poll is ready to be shared)
msgBotKbIconFg: msgServiceFg;

// bot inline keyboard button ripple effect
msgBotKbRippleBg: #61bdff0b;

// inbox media message status text (like in file that is being downloaded)
mediaInFg: msgInDateFg;

// inbox selected media message status text (like in file that is being downloaded)
mediaInFgSelected: msgInDateFgSelected;

// outbox media message status text (like in file that is being downloaded)
mediaOutFg: msgOutDateFg;

// outbox selected media message status text (like in file that is being downloaded)
mediaOutFgSelected: msgOutDateFgSelected;

// youtube play icon background (when a link to a youtube video with a webpage preview is sent)
youtubePlayIconBg: #e83131c8;

// youtube play icon arrow (when a link to a youtube video with a webpage preview is sent)
youtubePlayIconFg: windowFgActive;

// other video play icon background (like when a link to a vimeo video with a webpage preview is sent)
videoPlayIconBg: #0000007f;

// other video play icon arrow (like when a link to a vimeo video with a webpage preview is sent)
videoPlayIconFg: #ffffff;

// toast notification background (like when you click on your t.me link when editing your username)
toastBg: #000000b2;

// toast notification text (like when you click on your t.me link when editing your username)
toastFg: windowFgActive;

// report spam panel background (like a non contact user writes your for the first time)
reportSpamBg: #3c4a57;

// report spam panel text (when you send a report from that panel)
reportSpamFg: windowFg;

// arrow button background (to scroll to the end of the viewed chat)
historyToDownBg: #0a2b4c;

// arrow button background with mouse over
historyToDownBgOver: #0f355c;

// arrow button ripple effect
historyToDownBgRipple: #163f68;

// arrow button icon
historyToDownFg: #d7e8f5;

// arrow button icon with mouse over
historyToDownFgOver: menuIconFgOver;

// arrow button shadow
historyToDownShadow: #00000040;

// history compose area background (message write area / reply information / forwarding information)
historyComposeAreaBg: #0b2238;

// history compose area text
historyComposeAreaFg: historyTextInFg;

// history compose area text when replying to a media message
historyComposeAreaFgService: msgInDateFg;

// history compose area icon (like emoji, attach, bot command..)
historyComposeIconFg: menuIconFg;

// history compose area icon with mouse over
historyComposeIconFgOver: menuIconFgOver;

// send message icon
historySendIconFg: windowBgActive;

// send message icon with mouse over
historySendIconFgOver: windowBgActive;

// pinned message area background
historyPinnedBg: #0b2844;

// reply / forward / edit message area background
historyReplyBg: historyComposeAreaBg;

// reply / forward / edit message left icon
historyReplyIconFg: windowBgActive;

// reply / forward / edit message cancel button
historyReplyCancelFg: cancelIconFg;

// reply / forward / edit message cancel button with mouse over
historyReplyCancelFgOver: cancelIconFgOver;

// unblock / join channel / mute channel button background
historyComposeButtonBg: historyComposeAreaBg;

// unblock / join channel / mute channel button background with mouse over
historyComposeButtonBgOver: #0d2a46;

// unblock / join channel / mute channel button ripple effect
historyComposeButtonBgRipple: #123151;

// shared files / links checkbox background for not selected rows when some rows are selected
overviewCheckBg: #00000040;

// shared files / links checkbox icon for not selected rows when some rows are selected
overviewCheckFg: #ffffff;

// shared files / links checkbox icon for selected rows
overviewCheckFgActive: #ffffff;

// shared photos / videos / links fill for selected rows
overviewPhotoSelectOverlay: #01a2ff33;

// group members list in group profile user last seen text with mouse over
profileStatusFgOver: #6592b7;

// profile verified check icon background
profileVerifiedCheckBg: windowBgActive;

// profile verified check icon tick
profileVerifiedCheckFg: #ffffff;

// group members list admin star icon
profileAdminStartFg: #0392ff;

// custom notifications settings box monitor color
notificationsBoxMonitorFg: windowFg;

// #6389a8; // custom notifications settings box monitor screen background
notificationsBoxScreenBg: dialogsBgActive;

// custom notifications settings box small sample userpic placeholder
notificationSampleUserpicFg: windowBgActive;

// custom notifications settings box small sample close button placeholder
notificationSampleCloseFg: #d7d7d7;

// custom notifications settings box small sample text placeholder
notificationSampleTextFg: #d7d7d7;

// custom notifications settings box small sample name placeholder
notificationSampleNameFg: #939393;

// change phone number box left simcard icon
changePhoneSimcardFrom: notificationSampleTextFg;

// change phone number box right simcard and plane icons
changePhoneSimcardTo: notificationSampleNameFg;

// main menu background
mainMenuBg: windowBg;

// main menu top cover background
mainMenuCoverBg: #017aca;

// main menu top cover text
mainMenuCoverFg: windowFgActive;

// 
mainMenuCloudFg: activeButtonFg;

// 
mainMenuCloudBg: #0168a9;

// audio file player background
mediaPlayerBg: windowBg;

// audio file player playback progress already played part
mediaPlayerActiveFg: windowBgActive;

// audio file player playback progress upcoming (not played yet) part with mouse over
mediaPlayerInactiveFg: sliderBgInactive;

// audio file player loading progress (when you're playing an audio file and switch to the previous one which is not loaded yet)
mediaPlayerDisabledFg: #69cdff;

// file rectangle background (when you view a png file in Media Viewer and go to a previous, not loaded yet, file)
mediaviewFileBg: windowBg;

// file name in file rectangle
mediaviewFileNameFg: windowFg;

// file size text in file rectangle
mediaviewFileSizeFg: windowSubTextFg;

// red file thumbnail placeholder corner in file rectangle (for a file without thumbnail, like .pdf)
mediaviewFileRedCornerFg: #d55959;

// yellow file thumbnail placeholder corner in file rectangle (for a file without thumbnail, like .zip)
mediaviewFileYellowCornerFg: #e8a659;

// green file thumbnail placeholder corner in file rectangle (for a file without thumbnail, like .exe)
mediaviewFileGreenCornerFg: #49a957;

// blue file thumbnail placeholder corner in file rectangle (for a file without thumbnail, like .dmg)
mediaviewFileBlueCornerFg: #059bff;

// file extension text in file thumbnail placeholder in file rectangle
mediaviewFileExtFg: activeButtonFg;

// context menu in Media Viewer background
mediaviewMenuBg: #383838;

// context menu item background with mouse over
mediaviewMenuBgOver: #505050;

// context menu item ripple effect
mediaviewMenuBgRipple: #676767;

// context menu item text
mediaviewMenuFg: windowFgActive;

// Media Viewer background
mediaviewBg: #222222eb;

// Media Viewer background when viewing a video in full screen
mediaviewVideoBg: imageBg;

// controls background (like next photo / previous photo)
mediaviewControlBg: #0000003c;

// controls icon (like next photo / previous photo)
mediaviewControlFg: windowFgActive;

// caption text background (when viewing photo with caption)
mediaviewCaptionBg: #11111180;

// caption text
mediaviewCaptionFg: mediaviewControlFg;

// caption text link
mediaviewTextLinkFg: #119cff;

// save to file toast message background in Media Viewer
mediaviewSaveMsgBg: toastBg;

// save to file toast message text
mediaviewSaveMsgFg: toastFg;

// video playback progress already played part
mediaviewPlaybackActive: #c7c7c7;

// video playback progress upcoming (not played yet) part
mediaviewPlaybackInactive: #252525;

// video playback progress already played part with mouse over
mediaviewPlaybackActiveOver: #ffffff;

// video playback progress upcoming (not played yet) part with mouse over
mediaviewPlaybackInactiveOver: #474747;

// video playback progress text
mediaviewPlaybackProgressFg: #ffffffc7;

// video playback controls icon
mediaviewPlaybackIconFg: mediaviewPlaybackActive;

// video playback controls icon with mouse over
mediaviewPlaybackIconFgOver: mediaviewPlaybackActiveOver;

// transparent filling part (when viewing a transparent .png file in Media Viewer)
mediaviewTransparentBg: #ffffff;

// another transparent filling part
mediaviewTransparentFg: #cccccc;

// custom notification window background
notificationBg: windowBg;

// phone call popup background
callBg: #101b28f5;

// phone call popup name text
callNameFg: #ffffff;

// phone call popup emoji fingerprint background
callFingerprintBg: #00000066;

// phone call popup status text
callStatusFg: #dfe1e3;

// phone call popup answer, hangup and mute mic icon
callIconFg: #ffffff;

// phone call popup answer button background
callAnswerBg: #0195ff;

// phone call popup answer button ripple effect
callAnswerRipple: #018dff;

// phone call popup answer button outer ripple effect
callAnswerBgOuter: #0184ff26;

// phone call popup hangup button background
callHangupBg: #cc4646;

// phone call popup hangup button ripple effect
callHangupRipple: #ca4141;

// phone call popup line busy cancel button background
callCancelBg: #ffffff;

// phone call popup line busy cancel button icon
callCancelFg: #517495;

// phone call popup line busy cancel button ripple effect
callCancelRipple: #3a5f82;

// phone call popup mute mic ripple effect
callMuteRipple: #ffffff12;

// active phone call bar background
callBarBg: #0168c2;

// active phone call bar mute and hangup button ripple effect
callBarMuteRipple: #077fe1;

// phone call bar with muted mic background
callBarBgMuted: #1f4e7a;

// phone call bar with muted mic mute and hangup button ripple effect
callBarUnmuteRipple: #2b6397;

// phone call bar text and icons
callBarFg: dialogsNameFgActive;

//
importantTooltipBg: toastBg;

//
importantTooltipFg: toastFg;

//
importantTooltipFgLink: #0191ff;

// default rounded input field active background (like in chats list search field when field is focused)
filterInputActiveBg: #173250;

// bot keyboard button background
botKbBg: #173250;

// bot keyboard button ripple effect
botKbDownBg: #203f60;

// emoji category icon
emojiIconFg: #7995ad;

// active emoji category icon
emojiIconFgActive: #0191ff;

// shared media round checkbox border
overviewCheckBorder: #ebf6ff;

// filters side bar background
sideBarBg: #00132b;

// filters side bar active background
sideBarBgActive: #193351;

// filters side bar ripple effect
sideBarBgRipple: #132943;

// filters side bar text
sideBarTextFg: #75a8d0;

// filters side bar active item text
sideBarTextFgActive: #0195ff;

// filters side bar icon
sideBarIconFg: #75a8d0;

// filters side bar active item icon
sideBarIconFgActive: #0199ff;

// filters side bar badge background
sideBarBadgeBg: #0199ff;

// filters side bar unimportant badge background
sideBarBadgeBgMuted: #75a8d0;

// inactive area in footer of statistic charts
statisticsChartInactive: #264364c8;

// sides in footer of statistic charts
statisticsChartActive: #47769fd8;
