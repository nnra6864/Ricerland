# Prevents windows from maximizing themeselves
windowrule = suppressevent maximize, class:.*
windowrule = suppressevent maximize fullscreen,class:cs2
windowrule = suppressevent maximize fullscreen,class:csgo_linux64
windowrule = suppressevent maximize fullscreen,class:steam_app_730

# Opaque
windowrule = opaque, class:vlc.*
windowrule = opaque, class:mpv.*
windowrule = opaque, class:viewnior.*
windowrule = opaque, class:krita.*
windowrule = opaque, initialTitle:PixiEditor
windowrule = opaque, class:kdenlive.*
windowrule = opaque, class:org.kde.kdenlive.*
windowrule = opaque, class:blender.*
windowrule = opaque, title:Grayjay.*
windowrule = opaque, class:FreeTube.*
windowrule = opaque, class:zen
windowrule = opaque, class:blur-Linux-Release-x64.*
windowrule = opaque, class:Unity, initialTitle:Unity
windowrule = opaque, initialTitle:Flax Editor.*
windowrule = opaque, class:REAPER
windowrule = opaque, class:resolve, title:DaVinci Resolve.*
windowrule = opaque, class:Material Maker, initialTitle:Godot

# Opaque(Transparency can be rendered by the app)
#windowrule = opaque, class:equibop
#windowrule = opaque, class:kitty
#windowrule = opaque, class:com.mitchellh.ghostty
#windowrule = opaque, class:com.ghostty.ncmpcpp
#windowrule = opaque, class:org.telegram.desktop
#windowrule = opaque, class:dev.zed.Zed

# Tearing
windowrule = immediate, class:cs2 # CS2
windowrule = immediate, class:steam_app_730 # CSGO/CO/CC
windowrule = immediate, class:hl_linux # CS 1.6
windowrule = immediate, class:osu! # Osu

# Social Workspace
$socialWorkspace = 2
windowrule = workspace $socialWorkspace silent, class:info.mumble.Mumble
windowrule = workspace $socialWorkspace silent, class:equibop
windowrule = workspace $socialWorkspace silent, class:Revolt
windowrule = workspace $socialWorkspace silent, class:Element
windowrule = workspace $socialWorkspace silent, class:vesktop
windowrule = workspace $socialWorkspace silent, class:org.telegram.desktop
windowrule = workspace $socialWorkspace silent, class:nheko

# Special Workspace
$specialWorkspace = special
windowrule = workspace $specialWorkspace silent, class:^org\.keepassxc\.KeePassXC$, initialTitle:^.*\[Locked\] - KeePassXC$
windowrule = workspace $specialWorkspace silent, class:com.ghostty.ncmpcpp
windowrule = workspace $specialWorkspace silent, class:obsidian
windowrule = workspace $specialWorkspace silent, initialClass:com.obsproject.Studio

# Nisualizer
#windowrule = minsize 5120 1440, class:Nisualizer.x86_64
windowrule = size 100% 100%, class:Nisualizer.x86_64
windowrule = nofocus, class:Nisualizer.x86_64
windowrule = opaque, class:Nisualizer.x86_64
windowrule = rounding 0, class:Nisualizer.x86_64

# GLava
windowrule = noblur, class:GLava

# Jetbrains
windowrule = nofocus, class:jetbrains.*, title:win.* # Prevents tooltips from stealing focus

# Clipse
windowrule = float, title:clipse

# Material Maker
windowrule = tile, class:Material Maker, initialTitle:Godot
windowrule = float, class:Material Maker, title:Alert!

# Dunst
layerrule = blur, notifications
layerrule = ignorealpha 0, notifications
layerrule = animation slide, notifications

# Rofi
layerrule = blur, rofi
layerrule = ignorealpha 0, rofi
layerrule = animation slide, rofi

# SwayNC
layerrule = blur, swaync-control-center
layerrule = ignorealpha 0, swaync-control-center
layerrule = blur, swaync-notification-window

# Transitioner
layerrule = noanim, Transitioner

# Slurp(fixes screenshots being white)
layerrule = noanim, selection

# Hints
layerrule = noanim, hints

# XWaylandBridge
windowrule = opacity 0.0 override, class:xwaylandvideobridge
windowrule = noanim, class:xwaylandvideobridge
windowrule = noinitialfocus, class:xwaylandvideobridge
windowrule = maxsize 1 1, class:xwaylandvideobridge
windowrule = noblur, class:xwaylandvideobridge
