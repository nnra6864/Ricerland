# Prevents windows from maximizing/fullscreening themeselves
windowrule = suppress_event maximize, match:class .*
windowrule = suppress_event maximize fullscreen, match:class cs2
windowrule = suppress_event maximize fullscreen, match:class csgo_linux64
windowrule = suppress_event maximize fullscreen, match:class steam_app_730
windowrule = suppress_event maximize fullscreen, match:class hl_linux
windowrule = suppress_event maximize fullscreen, match:title Beat Saber

# Transparency (some apps don't support native transparency)
windowrule = tag transparent, match:class org.kde.dolphin
windowrule = tag transparent, match:class org.kde.kdeconnect.*
windowrule = tag transparent, match:class org.kde.partitionmanager
windowrule = tag transparent, match:class org.qbittorrent.qBittorrent
windowrule = tag transparent, match:class org.fkoehler.KTailctl
windowrule = tag transparent, match:class org.openrgb.OpenRGB
windowrule = tag transparent, match:class org.keepassxc.KeePassXC
windowrule = tag transparent, match:class PrismLauncher
windowrule = tag transparent, match:class steam
windowrule = opacity 1, match:tag transparent

# Broken Opaque (some apps don't behave weirdly when opaque if a transparent theme is used)
windowrule = tag broken_opaque, match:class info.mumble.Mumble
windowrule = tag broken_opaque, match:class obsidian
windowrule = opacity 0.99999, match:tag broken_opaque

# Tearing
windowrule = immediate true, match:class cs2 # CS2
windowrule = immediate true, match:class csgo_linux64
windowrule = immediate true, match:class steam_app_730 # CSGO/CS:CO/CC/CS:R
windowrule = immediate true, match:class hl_linux # CS 1.6
windowrule = immediate true, match:class osu! # Osu
windowrule = immediate true, match:title Beat Saber

# Floating
windowrule = float true, match:class waydroid.com.*
windowrule = float true, match:class hyprland-share-picker
windowrule = float true, match:class org.keepassxc.KeePassXC, match:title KeePassXC -  Access Request

# Fullscreen
windowrule = fullscreen 1, match:class com.moonlight_stream.Moonlight

# Nisualizer
windowrule {
    name = Nisualizer
    match:class = Nisualizer.x86_64

    size = 100% 100%
    no_focus = true
    rounding = 0
    #minsize = 5120 1440
}

# Jetbrains
windowrule = match:class jetbrains.*, match:title win.*, no_focus true # Prevents tooltips from stealing focus

# Material Maker
windowrule = match:class Material Maker, match:initial_title Godot, tile true
windowrule = match:class Material Maker, match:title Alert!, float true

# Dunst
layerrule = match:namespace notifications, blur true
layerrule = match:namespace notifications, ignore_alpha 0
layerrule = match:namespace notifications, animation slide

# wlr-which-key
layerrule = match:namespace wlr_which_key, blur true
layerrule = match:namespace wlr_which_key, ignore_alpha 0

# Hyprlauncher
layerrule = match:namespace hyprlauncher, blur true
layerrule = match:namespace hyprlauncher, ignore_alpha 0
layerrule = match:namespace hyprlauncher, animation slide

# Rofi
layerrule = match:namespace rofi, blur true
layerrule = match:namespace rofi, ignore_alpha 0
layerrule = match:namespace rofi, animation slide

# SwayNC
layerrule = match:namespace swaync-control-center, blur true
layerrule = match:namespace swaync-control-center, ignore_alpha 0
layerrule = match:namespace swaync-control-window, blur true

# Transitioner
layerrule = match:namespace Transitioner, no_anim true

# Slurp(fixes screenshots being white)
layerrule = match:namespace selection, no_anim true

# Hints
layerrule = match:namespace hints, no_anim true

# Vicinae
layerrule {
    name = vicinae-blur
    blur = on
    ignore_alpha = 0.05
    animation = slide bottom
    match:namespace = vicinae
}

# XWaylandVideoBridge
windowrule {
    name = XWaylandVideoBridge
    match:class = xwaylandvideobridge

    no_initial_focus = true
    no_focus = true
    no_anim = true
    no_blur = true
    max_size = 1 1
    opacity = 0.0
}
